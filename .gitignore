node_modules/
.expo/
dist/
npm-debug.*
*.jks
*.p8
*.p12
*.key
*.mobileprovision
*.orig.*
web-build/
.DS_Store
*.pem
android/
ios/
.env
.env.local

# Firebase service-account / admin SDK private keys — SENSITIVE, never commit.
# (google-services.json is NOT matched here and is safe to commit.)
*serviceAccount*.json
*service-account*.json
*-firebase-adminsdk-*.json
