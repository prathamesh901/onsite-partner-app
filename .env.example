# Copy this file to `.env` and fill in the values below.
#
# Supabase project (already provisioned — do not change the URL).
EXPO_PUBLIC_SUPABASE_URL=https://ujwnukabzpztykdwoxxo.supabase.co

# >>> PASTE YOUR SUPABASE ANON / PUBLISHABLE KEY HERE <<<
# Find it in the Supabase dashboard: Project Settings -> API -> "anon public".
EXPO_PUBLIC_SUPABASE_ANON_KEY=

# Backend REST API base URL (already live — do not change).
EXPO_PUBLIC_API_BASE_URL=https://onsite-partner-backend.vercel.app
